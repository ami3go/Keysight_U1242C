# Support

For issues, include:

- OS and Python version
- pyserial version
- serial adapter/COM port
- exact command or example used
- log output with `--verbose` when possible
- whether `u1242c-log --hardware-check --port <PORT>` succeeds

Do not post confidential test data in public issue trackers.
